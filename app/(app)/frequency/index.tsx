import FrequencyScreen from "@/components/frequency";

const Frequency = ({ showNavigation }: { showNavigation: boolean }) => {
  return <FrequencyScreen showNavigation={showNavigation} />;
};

export default Frequency;
